[
  {
    "description": "Create a work item. Returns the created row (including node_id and the serial wi_number, which are the same number since the 0009 identity migration).",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "area": {
          "description": "Area name — the alternative to `area_id`, resolved within the item's project.",
          "type": [
            "string",
            "null"
          ]
        },
        "area_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "content": {
          "type": "string"
        },
        "details": {
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "sprint": {
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        },
        "wi_status": {
          "default": "open",
          "enum": [
            "open",
            "resolved",
            "done",
            "closed",
            "parked"
          ],
          "type": "string"
        },
        "wi_tshirt": {
          "default": "Unknown",
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "Huge",
            "Unknown"
          ],
          "type": "string"
        },
        "wi_type": {
          "default": "task",
          "enum": [
            "task",
            "bug",
            "chore",
            "feature",
            "research",
            "tweak",
            "brainstorm",
            "maintenance"
          ],
          "type": "string"
        }
      },
      "required": [
        "content",
        "title"
      ],
      "type": "object"
    },
    "name": "create_work_item"
  },
  {
    "description": "The work-item list: lean rows (wi_number, node_id, project, title, wi_type, wi_status, wi_tshirt, comment_count, has_details, has_handoff, proposal_node_id, updated -- no content/details), ordered by wi_number, as {items, total, limit, offset, omitted}. `omitted` is {closed, archived} -- the rows the defaults hid, so a narrowed list can never be mistaken for the whole corpus. `updated` is when the item last changed (korg #1411): the staleness signal a sweep needs without one get_work_item per row -- wi_number already proxies creation age, this is the last-touched half. Defaults: everything not terminal (`open`, `resolved`, `done`, `parked`) and unarchived. Pass wi_status:\"all\" (or one status, including \"closed\") for the rest, `project` (name) to scope to one project. There is no detail flag: reading one item in full is get_work_item, which also inlines its comments and edges.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "project": {
          "description": "Project name to filter by",
          "type": [
            "string",
            "null"
          ]
        },
        "wi_status": {
          "description": "Which rows. Omit for everything not terminal (`open`, `resolved`,\n`done`, `parked`); `\"all\"` to include `closed` too; one status for\nexactly that.",
          "enum": [
            "open",
            "resolved",
            "done",
            "closed",
            "parked",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_work_items"
  },
  {
    "description": "Fetch a single work item by `wi_number` OR `node_id` -- for a work item they are the SAME number (the 0009 identity migration), so pass whichever you are holding; passing both is invalid_input rather than a guess. isError with code `not_found` if there is none. Comments are inlined (up to 10; `comments_truncated:true` + `comment_count` signal a longer thread — fetch the whole thread with list_comments, which returns every comment in one unpaginated array). Comments often hold the real payload (resolution rationale, decisions), so prefer this over list_work_items when you need the full state of one item.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "description": "The alternative spelling of `wi_number`, not a second identity: since\nthe 0009 migration a work item's node id and `wi_number` are the same\nvalue. Pass one or the other, never both.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "wi_number": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "get_work_item"
  },
  {
    "description": "Partially update a work item by its wi_number; returns the updated row (isError with code `not_found` if the wi_number does not exist). Only the fields you pass are changed. Status lifecycle: open -> resolved (implemented; may still need a user test or PR) -> done (agent satisfied; terminal but visible in default lists) -> closed (reserved for Ken; hidden from list_work_items unless you pass wi_status \"closed\" or \"all\" -- do not set unless directed). `parked` is off that line entirely (korg #810): deferred indefinitely, still visible in the default list because the point is keeping it in view, and never finished -- a dependency on a parked item stays an unmet blocker. For nullable fields (project_id, details, sprint, area_id, parent, category) pass null to clear or omit to leave unchanged. Moving projects (project_id) clears an area that no longer belongs to the target project unless you pass a valid area_id in the same call.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "area": {
          "description": "Area name — the alternative to `area_id`; null clears. Resolved in the new project.",
          "type": [
            "string",
            "null"
          ]
        },
        "area_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "content": {
          "type": [
            "string",
            "null"
          ]
        },
        "details": {
          "type": [
            "string",
            "null"
          ]
        },
        "parent": {
          "description": "Parent work item's wi_number; null clears the parent.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "project": {
          "description": "Project name — the alternative to `project_id`; null unassigns. Never pass both.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "Move to this project (id); null unassigns. Get ids from list_projects.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "sprint": {
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        },
        "wi_number": {
          "format": "int64",
          "type": "integer"
        },
        "wi_status": {
          "enum": [
            "open",
            "resolved",
            "done",
            "closed",
            "parked"
          ],
          "type": "string"
        },
        "wi_tshirt": {
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "Huge",
            "Unknown"
          ],
          "type": "string"
        },
        "wi_type": {
          "enum": [
            "task",
            "bug",
            "chore",
            "feature",
            "research",
            "tweak",
            "brainstorm",
            "maintenance"
          ],
          "type": "string"
        }
      },
      "required": [
        "wi_number"
      ],
      "type": "object"
    },
    "name": "update_work_item"
  },
  {
    "description": "korg's one time-series read (#1318): daily work-item flow, one row per day in the board's timezone, oldest first, ending today -- {day, added, closed, backlog, added_durable, closed_durable} per row, plus {backlog_before, horizon, timezone, durable_after_days, generated} on the envelope. `added` counts creations, from node.created (complete for every item; creating writes no transition row, so a log-derived added would be silently zero). `closed` counts moves to `closed`, from the transition log. `backlog` is the open (non-closed, unarchived) count at that day's end, reconstructed from the log -- today's value equals list_work_items' `total`. The `*_durable` split is where the signal is: an item is durable once it lived (or has so far lived) more than `durable_after_days` days; everything faster is sprint-internal churn, which is 3 of every 4 closes, so the raw added/closed pair reads ~1:1 forever and says nothing about the backlog. Note `added_durable` is structurally zero for the newest `durable_after_days` days -- durability is only knowable in retrospect, so at the 10-day default a summed `added_durable` covers only the OLDEST part of the window while a summed `closed_durable` covers all of it; printing the two side by side without saying so compares unlike figures. `backlog_before` is the open count at the end of the day BEFORE `days[0]` -- the window's baseline, and the one number the series cannot supply, because every row's `backlog` is that day's END. Take the window delta as `days.last().backlog - backlog_before`, NOT as `days.last().backlog - days[0].backlog`: the latter measures one day less than the `added`/`closed` sums do and is off by the first day's own net. It is null when the window starts at `horizon` (no prior day the log can answer) -- null, never 0, since a 0 is a real backlog level. `days` defaults to 10. A window reaching past `horizon` (where the transition log begins; it was deliberately not backfilled) is CLAMPED: the series starts at the horizon and is simply shorter than asked -- days the log cannot answer are absent, never zero-filled, because a zero that means 'no data' renders exactly like 'nothing closed'. Take the series length from the response, not from the default you passed.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "days": {
          "default": 10,
          "description": "How many days back from today, in the board's timezone. A window reaching past the transition horizon is clamped to the horizon — short series, never zero-filled days.",
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "name": "work_item_flow"
  },
  {
    "description": "Create a kanban card. Returns the created card row.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "default": "",
          "type": "string"
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "default": 0,
          "type": "number"
        },
        "status": {
          "default": "Backlog",
          "enum": [
            "Backlog",
            "Research",
            "OnDeck",
            "Active",
            "Done",
            "Cut"
          ],
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "type": "object"
    },
    "name": "create_card"
  },
  {
    "description": "Partially update a kanban card by its node_id; returns the updated card (isError with code `not_found` if that node is missing or is not a card). Projects are addressed by `project_id` here and over REST alike (get ids from list_projects) -- REST used to take a project *name* and silently create it. Only the fields you pass are changed (move status/rank, edit title/description, archive, reassign project). For nullable fields (project_id, category) pass null to clear or omit to leave unchanged.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "project": {
          "description": "Project name — the alternative to `project_id`; null unassigns. Never pass both.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "type": "number"
        },
        "status": {
          "enum": [
            "Backlog",
            "Research",
            "OnDeck",
            "Active",
            "Done",
            "Cut"
          ],
          "type": "string"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_card"
  },
  {
    "description": "List cards as {items, total, limit, offset}, ordered by status, then rank, then node_id. Each row includes `comment_count`. Archived cards are EXCLUDED by default.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "project": {
          "description": "Project name to filter by",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "Backlog",
            "Research",
            "OnDeck",
            "Active",
            "Done",
            "Cut",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_cards"
  },
  {
    "description": "List the comments on a node -- ANY kind, not just work items and cards -- oldest first. Since sprint 055 every kind's UI surface renders them, so a proposal's, program's or report's thread is here too. Unpaginated: this is the whole thread, which is what get_work_item's inlined-comment cap points you at.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "list_comments"
  },
  {
    "description": "Add a comment to a node of any kind. Returns the created comment; isError with code `not_found` if the node does not exist.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "minLength": 1,
          "type": "string"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "body",
        "node_id"
      ],
      "type": "object"
    },
    "name": "add_comment"
  },
  {
    "description": "Delete a comment by its id. Returns {deleted: bool} — false means there was no such comment.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "name": "delete_comment"
  },
  {
    "description": "Edit a comment's body by its id (from list_comments). `created` is preserved; `updated` advances.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "minLength": 1,
          "type": "string"
        },
        "id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "body",
        "id"
      ],
      "type": "object"
    },
    "name": "update_comment"
  },
  {
    "description": "Capture a reading-list URL. Returns the created link row.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "name": "create_link"
  },
  {
    "description": "List reading-list links as {items, total, limit, offset}, ordered by node_id. Archived links are EXCLUDED by default. Each row carries `created` and `updated`, so capture recency is readable without a second call.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "disposition": {
          "enum": [
            "Unread",
            "Done",
            "Revisit",
            "Summarized",
            "VaultSaved",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "read": {
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_links"
  },
  {
    "description": "Update a reading-list link in ONE transaction: disposition, read flag, tags, archived -- any combination. This is how an agent records what it decided about a captured URL (migration 0004's intended workflow), and `archived` is its lifecycle end: the disposal list_links has always excluded by default. Returns the updated link; isError `not_found` if the node is missing or is not a link; an invalid disposition changes nothing.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "description": "The lifecycle end for a link that was real (WI #888). `list_links` has\ndocumented an archived-excluded default since #534 with no write able\nto reach it; this is that write. For a capture that was never real —\na probe, a mistyped URL — `delete_link` is the honest disposal.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "disposition": {
          "enum": [
            "Unread",
            "Done",
            "Revisit",
            "Summarized",
            "VaultSaved"
          ],
          "type": "string"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "read": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_link"
  },
  {
    "description": "Hard-delete a captured URL -- the disposal for a link that was never real (a probe, a mistyped capture). For a link that WAS real, archive it with update_link instead; delete is irreversible. REFUSES with `conflict` if the link has any relationship edge or comment, naming what points at it -- anything referenced has a history, and resolving that reference is your decision, not the database's. Returns {deleted: bool} -- false means there was no such link.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "delete_link"
  },
  {
    "description": "DEPRECATED -- use update_link, which does this plus disposition and tags in one transaction. Marks a reading-list link read or unread; returns the updated link.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "read": {
          "type": "boolean"
        }
      },
      "required": [
        "node_id",
        "read"
      ],
      "type": "object"
    },
    "name": "mark_link_read"
  },
  {
    "description": "Create a relationship edge between any two nodes. The label reads left-to-right. The vocabulary is CLOSED -- these labels and no others: `covers` (proposal -> work item), `includes` (program -> proposal), `finding` (report -> work item), `depends_on` (dependent -> dependency), `materializes` (schedule -> work item: a schedule's generation history, normally written by materialize_schedule -- `last_wi_id` points only at the newest, the edges are the record of every one), `has_handoff` (node -> handoff; normally written by create_handoff, but relate attaches an existing handoff to another node), and `has_attachment` (node -> attachment: what makes an uploaded image BELONG to something -- an attachment with no such edge is `pending` and the sweeper deletes it 24h after upload, so writing this edge rescues an image and unrelating one lets it go) are DIRECTED -- orientation is meaningful, and the reverse is a distinct edge (A depends_on B plus B depends_on A is a cycle, not a duplicate). `related-to` (the two nodes are related) and `collides-with` (same contract / fold on landing -- the kfdc curator's mined collisions, korg #1002) are UNDIRECTED -- orientation is stored but meaningless, so read them symmetrically. An unregistered label is invalid_input naming the registry and the near-miss. Endpoint kinds are validated wherever the registry pins them: `covers`, `includes`, `finding` and `materializes` pin BOTH ends, `has_handoff` pins its right end (it must be a handoff) and `has_attachment` its right end (an attachment); `covers` additionally requires BOTH ENDS IN THE SAME PROJECT -- a proposal bundles work from one project, and cross-project work belongs to a program, the layer above proposals (korg #968). `includes` is the layer where cross-project work is legal, so it has NO same-project rule; `depends_on` across projects likewise stays legal and is the normal case. Exact duplicates dedup, and relating the reverse of an undirected edge returns the existing one. Optionally pass `rank` -- the position of the right endpoint within the left one, which is how a program's slices are ordered; re-relating an existing edge WITH a rank moves it in place and keeps its provenance, and re-relating without one leaves the position alone (so reordering never needs unrelate + relate). Optionally pass `origin` -- self-reported provenance (e.g. your skill name); it is recorded, not verified. Both endpoints must exist (isError `not_found`) and must differ (isError `invalid_input` -- self-edges are rejected).",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "minLength": 1,
          "type": "string"
        },
        "left": {
          "format": "int64",
          "type": "integer"
        },
        "origin": {
          "description": "Self-reported provenance (D-17): who is writing this edge — the web\nclient sends `\"web\"`, a skill sends its name. Optional and unverified\n(korg is no-auth HTTP); absent means unknown, stored NULL.",
          "type": [
            "string",
            "null"
          ]
        },
        "rank": {
          "description": "Position of the right endpoint within the left one — used by `includes`\nto order a program's slices (#968). Lower sorts first; unranked edges\nsort last.\n\nRe-relating an existing edge **with** a rank moves it in place, keeping\nthe edge's `created`/`origin`; re-relating **without** one leaves the\nposition alone. That is the reorder path — `unrelate` + `relate` would\nwork too but throws the provenance away.",
          "type": [
            "number",
            "null"
          ]
        },
        "right": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "label",
        "left",
        "right"
      ],
      "type": "object"
    },
    "name": "relate"
  },
  {
    "description": "List the nodes linked to a node (any kind), with labels. Returns {items, total, limit, truncated}. Each item has `rel_id` (pass to `unrelate`), `direction` (\"out\" = the queried node is the edge's left, so the label reads queried->neighbor; \"in\" = the reverse) and `directed` -- when `directed` is false the label is registry-undirected (e.g. related-to) and you MUST treat the edge as symmetric, ignoring `direction`. Filter server-side with `label` and/or `kind` instead of pulling every edge: e.g. label=\"covers\", kind=\"workitem\" for a proposal's work items. Ordering is neighbor node_id then rel_id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "description": "Only neighbors of this node kind, e.g. \"workitem\".",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "Only edges with this relationship label.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "default": 100,
          "description": "Cap on returned edges; `truncated` says whether more matched.",
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "neighbors"
  },
  {
    "description": "Remove a relationship edge by its id (the `rel_id` from `neighbors`, or the id returned by `relate`). Returns {deleted: bool} — false means there was no such edge.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "name": "unrelate"
  },
  {
    "description": "Propose a sprint: bundle a title + summary with the work items it covers, in one call. `project` (or `project_id`) is REQUIRED, and every covered work item must be in it -- a proposal is a single-project bundle, so one from another project is invalid_input naming both projects, not a silent drop. Cross-project work is bundled by a program, the layer above proposals (korg #968). `summary` is a routing contract capped at 500 characters -- what the bundle is, why now, roughly how big -- and the analysis goes in `notes`, which is unbounded; an over-long summary is invalid_input, not a truncation. Returns the created proposal plus `covered` -- which of the given wi_numbers actually resolved. Numbers that do not resolve are dropped, so compare `covered` against your request.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "notes": {
          "description": "The analysis — measurements, dependencies, sequencing, alternatives\nconsidered. Unbounded, and the home for everything that will not fit\n`summary`. Returned by `get_proposal` and `list_proposals detail:\"full\"`,\nnever by the lean queue row.",
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "default": false,
          "type": "boolean"
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth, and **one of them is required**. Resolved by exact name, and an\nunknown name returns `not_found` rather than mis-filing, so pass a name\nyou are confident in directly. Call `list_projects` only when the name is\ngenuinely unknown or ambiguous — the roster in this server's\ninstructions already names every active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "**Required** — as either this or `project`, never both. A proposal is a\nsingle-project bundle (#967): it is what tells a session which repo to\nbranch in, and what `covers` is validated against.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "default": 0,
          "description": "Drag-order position; lower sorts first among unpinned proposals.",
          "type": "number"
        },
        "summary": {
          "description": "The routing contract. **≤500 characters** (rejected above that): what\nthis bundle is, why now, roughly how big. Written for a session choosing\na sprint from a queue, not for one that has already chosen it.",
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        },
        "work_item_numbers": {
          "default": [],
          "description": "wi_numbers this proposal covers; numbers that don't resolve are dropped.\nA number that resolves to a work item in a **different project** is not\ndropped — it is `invalid_input` naming both projects (#967), because\nsilently omitting the item you meant to include is the failure this rule\nexists to stop.",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        }
      },
      "required": [
        "summary",
        "title"
      ],
      "type": "object"
    },
    "name": "propose_sprint"
  },
  {
    "description": "Create or replace the daily report for (source, report_date). Same-day re-runs REPLACE both the content and the finding set -- findings you omit are unlinked -- but keep the node_id (links/comments survive). `findings_linked` echoes the wi_numbers that resolved; numbers that do not resolve are dropped, so compare it against your request.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "full markdown report",
          "type": "string"
        },
        "escalated": {
          "default": false,
          "type": "boolean"
        },
        "finding_work_items": {
          "default": [],
          "description": "wi_numbers of finding work items; numbers that don't resolve are dropped.",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "report_date": {
          "description": "YYYY-MM-DD",
          "format": "date",
          "type": "string"
        },
        "source": {
          "description": "reporter id, e.g. 'kmon'",
          "minLength": 1,
          "type": "string"
        },
        "status": {
          "enum": [
            "ok",
            "attention",
            "problem"
          ],
          "type": "string"
        },
        "summary": {
          "description": "one-liner for the list view",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "body",
        "report_date",
        "source",
        "status",
        "summary"
      ],
      "type": "object"
    },
    "name": "create_report"
  },
  {
    "description": "List daily reports, newest first (summary fields only). Pass `source` to filter.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "limit": {
          "default": 30,
          "type": "integer"
        },
        "source": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_reports"
  },
  {
    "description": "Fetch one report by node_id: full body plus linked finding work items. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_report"
  },
  {
    "description": "The sprint queue: pinned first, then rank, then node_id (a stable order -- equal ranks no longer shuffle between calls). Returns {items, omitted} -- NOT a bare array; `omitted` counts the rows the defaults hid ({done, declined, archived}), so a narrowed queue can never be mistaken for the whole corpus. Defaults: live proposals only (`proposed` + `active`), unarchived, and the lean projection -- node_id, title, status, project, rank, pinned, covered_count, comment_count, no `summary`. Pass status:\"all\" (or one status) for the rest, detail:\"full\" for every column including `summary` and `notes`. Reading one proposal is get_proposal, which also gives you the work items it covers.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detail": {
          "enum": [
            "lean",
            "full"
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name to filter by",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "proposed",
            "active",
            "done",
            "declined",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_proposals"
  },
  {
    "description": "Fetch one sprint proposal by node_id with everything needed to start it: the proposal fields (including `notes`, the full analysis the 500-character `summary` cannot hold), `covered` (the work items it covers -- wi_number, node_id, title, wi_status, wi_tshirt, project, comment_count -- ordered by wi_number), and inlined comments (up to 10, with `comments_truncated`). This replaces the old list_proposals + neighbors + list_work_items dance. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_proposal"
  },
  {
    "description": "Partially update a sprint proposal by its node_id; returns the updated proposal (isError with code `not_found` if that node is missing or is not a proposal). Only the fields you pass are changed. Use this for status transitions (proposed -> active -> done/declined), reordering (rank), pinning, or archiving. `summary` is capped at 500 characters (invalid_input above that) -- long-form analysis belongs in `notes`, which is unbounded; pass null to clear it.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "notes": {
          "description": "The analysis: measurements, dependencies, sequencing, what was\nconsidered and rejected. Unbounded; pass `null` to clear it.",
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "rank": {
          "type": "number"
        },
        "status": {
          "enum": [
            "proposed",
            "active",
            "done",
            "declined"
          ],
          "type": "string"
        },
        "summary": {
          "description": "The routing contract, **≤500 characters** (rejected above that). Put the\nanalysis in `notes` — it is unbounded, and that is what it is for.",
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_proposal"
  },
  {
    "description": "Create a program -- the CROSS-project layer (korg #968). A program bundles sprint PROPOSALS, ordered, and is the sanctioned home for work that spans repos; a proposal itself is strictly single-project. It takes NO project: passing `project`/`project_id` is invalid_input, because a program filed under one repo rebuilds exactly the mis-routing that rule cured. Its `span` -- which repos it touches -- is DERIVED from the projects of the proposals it includes, so it cannot go stale when you add a slice. `slices` are proposal node_ids IN ORDER (first = slice 1), written as ranked `includes` edges; an id that is not a proposal is refused outright rather than dropped, because a program's slice list is its plan. `aim` is the routing contract capped at 500 characters -- analysis goes in `notes`, which is unbounded.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "aim": {
          "description": "The routing contract — what this program is trying to achieve, across\nwhich repos, and how you will know it is done. Same job a proposal's\n`summary` does one level down.",
          "type": "string"
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "notes": {
          "description": "The analysis: sequencing, what each slice is waiting on, what was\nconsidered and rejected. Unbounded.",
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "default": false,
          "type": "boolean"
        },
        "project": {
          "description": "Present **only to be refused** (D-6). A program is the cross-project\nlayer; filing one under a single project rebuilds exactly the mis-routing\n#967 cured for proposals. Passing either selector is `invalid_input`\nnaming the rule, because a silent drop would leave the caller believing\nthe program is filed somewhere it is not.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "default": 0,
          "type": "number"
        },
        "slices": {
          "default": [],
          "description": "Proposal `node_id`s this program includes, **in the order given** — the\nfirst is slice 1. Ids that do not resolve to a proposal are refused, not\ndropped: a program's slice list is its plan, and a silently missing slice\nis a plan that lies.",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "aim",
        "title"
      ],
      "type": "object"
    },
    "name": "create_program"
  },
  {
    "description": "Fetch one program by node_id with the whole rollup, so a consumer NEVER crawls program -> proposals -> work items: the program fields, `span` (its derived projects), and `slices` -- the included proposals in program order, each with node_id, title, status, project, its position `rank`, `covered_count`, and one work-item count per status (open/resolved/done/closed/parked -- they sum to `covered_count`). Also carries inlined comments and the `related` block, where a `has_handoff` edge surfaces -- a program is precisely the kind that accrues one, and it is required context. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_program"
  },
  {
    "description": "List programs, pinned first then rank then node_id. Returns {items, omitted} -- NOT a bare array; `omitted` is {done, archived}, the rows the defaults hid. Defaults: live programs only (`active` + `holding`), unarchived. Pass status:\"all\" (or one status) for the rest. There is no detail flag and no project filter: a program has no project of its own (its `span` is derived and is on every row), and with a 500-character `aim` the full row is already the lean one. `holding` means started but nothing currently in flight -- it is the state a program spends most of its life in, and it is deliberately not collapsed into `active`.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "status": {
          "enum": [
            "active",
            "holding",
            "done",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_programs"
  },
  {
    "description": "Partially update a program by its node_id; returns the updated program (isError `not_found` if that node is missing or is not a program). Only the fields you pass change: title, aim, notes, status (active -> holding -> done), rank, pinned, archived, tags. `aim` is capped at 500 characters. Slice membership and ORDER are not here -- use relate/unrelate with the `includes` label, passing `rank` to position a slice.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "aim": {
          "description": "The routing contract, **≤500 characters**. Analysis goes in `notes`.",
          "type": [
            "string",
            "null"
          ]
        },
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "notes": {
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "rank": {
          "type": "number"
        },
        "status": {
          "enum": [
            "active",
            "holding",
            "done"
          ],
          "type": "string"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_program"
  },
  {
    "description": "Mark a node -- ANY kind: work item, proposal, program, card -- as awaiting Ken, or clear that marker. This is how an agent says \"this moves only when Ken acts\": a decision, an ops action no agent can perform, a review. Pass awaiting:true with a `note` saying what is being asked; pass awaiting:false to clear it, which you SHOULD do yourself once you have the answer rather than leaving an answered ask in the lane. Re-marking a node that is already awaiting KEEPS the original timestamp and only updates the note -- the age of an ask is what makes the lane useful, so it is not restarted. The marker is a real column, not a tag: tags are written wholesale and an unrelated tag edit would silently clear it. Clearing also happens automatically when Ken himself acts (a work item reaching `closed`, a proposal reaching `done`/`declined`, a program reaching `done`, a card reaching `Done`/`Cut` -- the kanban is his board end to end -- and anything archived).",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "awaiting": {
          "description": "`true` raises the marker, `false` clears it. Clearing is deliberately an\nagent-reachable operation (D-8): an agent that asked a question and got\nits answer in-session retracts its own marker rather than leaving an\nanswered ask in the lane until Ken clicks it away.",
          "type": "boolean"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "note": {
          "description": "What is being asked of Ken — the thing that makes the lane actionable\nwithout opening every row. Only valid with `awaiting: true`; re-setting\nwith a new note keeps the original `awaiting_since`, because the age of\nthe ask is the point.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "awaiting",
        "node_id"
      ],
      "type": "object"
    },
    "name": "set_awaiting"
  },
  {
    "description": "Everything currently waiting on Ken, oldest ask first -- one call, every node kind. Each row carries node_id, kind, wi_number (work items), a resolved title, project, the node's OWN status, `awaiting_since` and `awaiting_note`, so a board renders the lane without a follow-up read per row. Archived nodes and ones in a state only Ken sets (closed work items, `done`/`declined` proposals, done programs, `Done`/`Cut` cards) are excluded -- the lane holds live asks, never answered ones. Note that `resolved` and `done` work items DO appear: \"implemented, needs your user test\" is the canonical awaiting-Ken state.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    },
    "name": "list_awaiting"
  },
  {
    "description": "Full-text search across the WHOLE corpus in one call (korg #1177): every node kind that carries prose -- work items, proposals, programs, cards, reports, handoffs, schedules, links -- plus one document per COMMENT, which is where the answer usually is. A resolution comment, a review verdict, a recon note: none of that is reachable by scanning titles, and title-scanning is measurably the worse tool (9/12 vs 12/12 on the frozen acceptance suite). Reach for this INSTEAD of paging list_work_items and reading rows yourself.\n\nReturns {items, total, limit, offset, omitted, relaxed, parsed}. Each item is lean and routable: node_id, comment_id (set only for comments, so \"this item matched\" is distinguishable from \"someone said this on it\"), kind, `locator` (`WI-836`, `korg:1395`, `korg:1395#comment-777` -- the spelling Ken and skills already use), title, status, project, `score`, `updated`, and a bounded `snippet` centred on the matched terms rather than the head of the document. Bodies are NOT here by design -- follow the locator with get_work_item / get_proposal / get_report when the snippet is not enough.\n\nQuery semantics: ALL terms are required. When that matches nothing the query is relaxed to ANY term and `relaxed: true` says so -- always read that flag, because a relaxed answer is a broad one and its `total` counts any-term matches (frequently most of the corpus). Quoted \"exact phrases\" and a leading `-` to exclude both work; a query carrying an exclusion is NEVER relaxed, because `foo -bar` relaxed would invert what you asked for. `parsed` shows the tsquery Postgres actually ran (stemming applied, stop words dropped), which is how you diagnose a surprising result set without guessing.\n\nScoping: by default only LIVE rows are searched -- each kind's own terminal state is excluded, matching what its list read hides (`closed` work items, `done`/`declined` proposals, `done` programs, `Done`/`Cut` cards), and archived rows are excluded too. `omitted` is {terminal, archived}, counted as a cascade so nothing is double-counted, and it is how you know a narrow answer was narrowed. **Pass scope:\"all\" whenever you are chasing something that was decided and closed** -- which is most historical questions; the default is tuned for \"what is in flight\", not \"what did we conclude\". Narrow further with `kind` (one document kind) and `project` (a comment inherits its parent's project).\n\nRanking is fixed and deliberately not a knob. This is a LEXICAL search: it matches words, not meaning, so a query sharing no vocabulary with its answer will miss -- phrase it in the words the record would use.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "kind": {
          "description": "Restrict to one document kind.",
          "enum": [
            "workitem",
            "card",
            "link",
            "sprint_proposal",
            "report",
            "handoff",
            "program",
            "schedule",
            "comment",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "project": {
          "description": "Project name to filter by. A comment inherits its parent's project.",
          "type": [
            "string",
            "null"
          ]
        },
        "q": {
          "description": "Plain terms. All terms are required; when that matches nothing the\nquery is relaxed to any-term and the response says `relaxed: true`.\nQuoted `\"exact phrases\"` and a leading `-` to exclude both work, and a\nquery carrying an exclusion is never relaxed.",
          "minLength": 1,
          "type": "string"
        },
        "scope": {
          "description": "Which rows. Omit for live ones only — each kind's own terminal state is\nexcluded, matching what its list read hides. `\"all\"` searches\neverything, which is what you want when chasing a decision that was\nsettled and closed.",
          "enum": [
            "live",
            "all",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "q"
      ],
      "type": "object"
    },
    "name": "search"
  },
  {
    "description": "The whole board in ONE call (korg #970) -- what a dashboard needs, instead of the 17-call crawl the 2026-07-31 backlog review had to do. Takes NO arguments: it is one screen's state, and every filter it could offer is already decided by what the panels are. Returns {generated, active, queue, proposals_omitted, proposal_edges, blocked, programs, programs_omitted, awaiting, depth, reports, events, sources, due_schedules}. `active` and `queue` are the `active` and `proposed` proposals, pinned-first-then-rank, each with `summary`, a work-item rollup (covered_count plus one count per status: open/resolved/done/closed/parked, which sum to it) so progress renders without a call per proposal, and `synopsis` -- the newest comment opening with the ⟦curator⟧ marker (korg #1003, the machine-curated line; null when none). `proposals_omitted` is the usual {done, declined, archived}. `proposal_edges` (korg #1003) are every edge whose BOTH endpoints are active/queue rows -- {left, right, label, directed (from the registry -- read undirected labels symmetrically), origin, created}, the read surface for edge provenance, which is how curated edges (origin \"kfdc-curator\") stay distinguishable from human ones. `programs` are the live programs, each with its ordered `slices` exactly as get_program returns them. `blocked` (korg #978) is the DETERMINISTIC half of Deconfliction: every UNMET depends_on holding up a live row, one entry per (row, blocker) pair -- {proposal, via, dependent, dependent_wi_number, blocker, blocker_kind, blocker_wi_number, blocker_title, blocker_project, blocker_status, sequenced_by}. `via` is \"proposal\" when the row itself carries the edge and \"covered\" when one of its covered work items does; both count. Unfinished is derived from the vocabulary per kind, so a `done` work item does NOT block (that is the completion split, not the list-visibility one). One hop only, and an archived blocker does not block -- a row held blocked forever by something nobody will ever do is worse than one that frees up. `sequenced_by` names the live program whose ordered slices ALREADY express this dependency (kfdc #1070), so a render can drop what Operations is drawing anyway instead of showing the same data twice; null when there is none. `events` (korg #977) is the ticker: the newest 20 status transitions, newest first -- {node_id, kind, wi_number, title, project, from_status, to_status, at}. A write that does not CHANGE the status produces no event, which is the whole point: `node.updated` advances on any edit, so a feed built from it would date a proposal by its last tag edit. The log starts at migration 0026 and was deliberately NOT backfilled (there was no honest history to backfill from), so an empty ticker means \"nothing has moved since the migration\", never \"nothing has ever moved\". `awaiting` is list_awaiting's lane, `depth` is per-project queue depth (every project, with its `status` and `category` -- `category` is there so a consumer can drop the permanently-active `eval` residue bucket without a second read), `reports` the newest 5, and `sources` (korg #950) is every reporting source with its `freshness` (fresh|stale|retired|unrated) and what it `asserts` (ok|attention|problem|unknown) -- anything not `fresh` asserts `unknown`, never a last known status, which is the whole point: a stale GREEN is how korg lied about the fleet for 11 days in July 2026. `sources` is deliberately UNCAPPED where `reports` is 5 -- a cap could push a stale source off the board behind fresher ones, which inverts the failure it exists to catch. `due_schedules` (korg #1385) is what is DUE RIGHT NOW, soonest-due first: exactly `list_schedules(due_only:true)`'s rows, so due-ness keeps its single definition and the board inherits all three clauses -- active, interval elapsed, and no outstanding materialized item (a drill whose work item is already open does not nag from here). Uncapped for the same reason `sources` is: the row a cap would drop is the one that has waited longest. It exists because schedule 1112 sat due for nine days where nothing that gets looked at daily could say so. There is deliberately NO counters block -- live proposals is active+queue, shipped is proposals_omitted.done, awaiting is awaiting.length, projects is depth filtered by status; a counter that can disagree with the list beside it is a bug. `generated` is Postgres's clock, the same one every timestamp here came from, so ages computed against it are right. For one proposal's covered items, one program's detail, or anything paged, use that entity's own read.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    },
    "name": "get_board"
  },
  {
    "description": "Create a SCHEDULE -- a work-item template plus a cadence, for work that a DATE makes appear (korg #581). This is korg's answer to \"recheck this after the DST transition\" and \"the restore drill is due again\", neither of which could be filed before: prose on a done node is out of every default view, and a work item for something not due for three months makes the queue lie. `cadence` is once|weekly|fortnightly|monthly|quarterly|yearly -- `once` is not a special case, it is the cadence whose interval is ZERO, so its `anchor_at` simply IS its fire date. The set mixes two families and the distinction decides which value is right: `weekly` and `fortnightly` are whole WEEKS, so they preserve a weekday; every calendar-month cadence drifts off it (2026-08-08 is a Saturday, 2026-09-08 is a Tuesday). \"Every other Saturday\" is therefore `fortnightly`, never `monthly`. `anchor_at` is where the interval counts from: SEED IT with the real last-done date (a quarterly drill last run 2026-07-08 anchored there comes due 2026-10-08, not today). `anchor_mode` picks which event advances it -- `completed` (default; when the materialized work item reaches done/closed) or `created` (at materialization). `title` and `template` may use exactly these substitutions: {YEAR}, {MONTH}, {DAY}, {DATE}, {QUARTER}; any other {PLACEHOLDER} is invalid_input rather than rendering literally into a title months from now. Creating a schedule materializes NOTHING -- korg runs no scheduler; call materialize_schedule when the work starts.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "anchor_at": {
          "description": "When the interval starts counting. **Seed this with the real last-done\ndate** — the restore drill was last verified 2026-07-08, so a quarterly\nschedule anchored there comes due 2026-10-08 rather than immediately.\nOmitted means now, which is right for something being started today and\nwrong for anything with history.\n\nFor cadence `once` this IS the fire date.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "anchor_mode": {
          "default": "completed",
          "description": "Which event advances the anchor — see [`vocab::SCHEDULE_ANCHORS`].\nDefaults to `completed`, the style #581 listed first and the one that\nmatches how maintenance is actually reasoned about (\"it's been three\nmonths since we last *did* it\").",
          "enum": [
            "completed",
            "created"
          ],
          "type": "string"
        },
        "cadence": {
          "enum": [
            "once",
            "weekly",
            "fortnightly",
            "monthly",
            "quarterly",
            "yearly"
          ],
          "type": "string"
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "notes": {
          "description": "Why this schedule exists and what \"done\" means for it. Stays on the\nschedule; not copied into the materialised item.",
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name — the alternative to `project_id`, never both. A schedule\ncarries a project (unlike a program): maintenance is maintenance *of*\nsomething.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "template": {
          "description": "The template body of the materialised work item — the checklist, the\nprocedure, the link to the runbook. Substituted the same way.",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "description": "The **template** title of the work item this materialises. May carry the\nsubstitutions in [`vocab::SCHEDULE_SUBSTITUTIONS`], e.g.\n`\"korg restore drill - {MONTH} {YEAR} (Quarterly)\"`. An unrecognised\n`{PLACEHOLDER}` is refused, not rendered literally.",
          "minLength": 1,
          "type": "string"
        },
        "wi_tshirt": {
          "default": "Unknown",
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "Huge",
            "Unknown"
          ],
          "type": "string"
        },
        "wi_type": {
          "default": "maintenance",
          "description": "What the materialised item is filed as. Defaults to `maintenance`, the\ntype #581 asked for so automation can find generated items as a group.",
          "enum": [
            "task",
            "bug",
            "chore",
            "feature",
            "research",
            "tweak",
            "brainstorm",
            "maintenance"
          ],
          "type": "string"
        }
      },
      "required": [
        "cadence",
        "title"
      ],
      "type": "object"
    },
    "name": "create_schedule"
  },
  {
    "description": "Fetch one schedule by node_id: its template fields, the read-time `due` predicate with `due_at`, `preview_title` (what the title renders to RIGHT NOW, so you can see what materializing would create), `outstanding` (whether its last materialized item is still open), and `materialized` -- every work item it has ever produced, newest first, which is the answer to \"when was this drill actually run?\". Also inlined comments and the `related` block. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_schedule"
  },
  {
    "description": "List schedules, SOONEST-DUE FIRST -- the first row is the next thing that wants doing. Returns {items, omitted} -- NOT a bare array; `omitted` is {done, archived, not_due}, and `not_due` is what a due_only read hid, so \"nothing is due\" is never confusable with \"I filtered it out\". Defaults: live schedules (`active` + `paused`), unarchived. Pass due_only:true for just what is due now. `due` is computed on every read, never stored -- a stored due flag is a cached predicate over a clock, which is exactly the thing that goes stale silently. A schedule is due when it is active, its interval has elapsed since `anchor_at`, AND its last materialized work item is no longer unfinished (`open`, `resolved`, `parked`) -- while that item is unfinished, IT is the surface and the schedule does not compete with it. `parked` counts as outstanding on purpose: parking a materialized drill defers it, and re-firing the schedule would produce a duplicate of work somebody just deliberately deferred.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "due_only": {
          "default": false,
          "description": "Only schedules that are due **right now**. The whole read is a\nread-time predicate, so this is a filter rather than a stored flag —\nsee `repo::SCHEDULE_DUE_SQL`.",
          "type": "boolean"
        },
        "project": {
          "description": "Restrict to one project by name.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "active",
            "paused",
            "done",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_schedules"
  },
  {
    "description": "Partially update a schedule by node_id; returns the updated schedule (isError `not_found` if that node is missing or is not a schedule). Only the fields you pass change: title, template, notes, cadence, anchor_mode, anchor_at, status, wi_type, wi_tshirt, archived, tags. Two common moves: `status:\"paused\"` stops a schedule surfacing without losing its anchor or its history, and setting `anchor_at` defers a drill (or records that you did the maintenance outside korg) without touching its cadence.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "anchor_at": {
          "description": "Move the anchor by hand — e.g. after doing the maintenance outside korg,\nor to defer a drill a fortnight without touching its cadence.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "anchor_mode": {
          "enum": [
            "completed",
            "created"
          ],
          "type": "string"
        },
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "cadence": {
          "enum": [
            "once",
            "weekly",
            "fortnightly",
            "monthly",
            "quarterly",
            "yearly"
          ],
          "type": "string"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "notes": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "`paused` is how a schedule stops surfacing without losing its anchor or\nits history.",
          "enum": [
            "active",
            "paused",
            "done"
          ],
          "type": "string"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "template": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "minLength": 1,
          "type": [
            "string",
            "null"
          ]
        },
        "wi_tshirt": {
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "Huge",
            "Unknown"
          ],
          "type": "string"
        },
        "wi_type": {
          "enum": [
            "task",
            "bug",
            "chore",
            "feature",
            "research",
            "tweak",
            "brainstorm",
            "maintenance"
          ],
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_schedule"
  },
  {
    "description": "Turn a due schedule into a real work item -- the explicit write that stands in for the daily tick korg deliberately does NOT run (#581/#950: building korg's answer to time on an unattended scheduled job would repeat the failure #950 exists to fix). Renders the template's substitutions from Postgres's clock, creates the work item in the schedule's project with its `wi_type` (default `maintenance`, so automation can find generated items as a group), writes a `materializes` edge for provenance, advances the anchor if `anchor_mode` is `created`, and marks a `once` schedule done. Refuses a schedule that is not due (pass force:true to bring one forward, e.g. a restore drill after a schema change) and ALWAYS refuses one whose last item is still open -- force does not lift that, because a second copy of a drill already in flight is not something you can want.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "force": {
          "default": false,
          "description": "Materialize a schedule that is not yet due — e.g. a restore drill\nbrought forward because the schema shape changed. Never lifts the\noutstanding-item check: a second copy of a drill whose first copy is\nstill open is not something a caller can want.",
          "type": "boolean"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "materialize_schedule"
  },
  {
    "description": "Every known report source and whether korg can currently BELIEVE it (korg #950) -- most-alarming first. A source that filed daily and then stopped is itself the alert: in July 2026 kmon crashed 1s into collection for 11 straight days and korg kept serving its last GREEN, so the fleet read healthy BECAUSE the thing checking it was broken. Each row carries `freshness` (fresh|stale|retired|unrated) and `asserts` (ok|attention|problem|unknown). THE RULE: anything not `fresh` asserts `unknown` -- never the last known status, because restating a stale GREEN is the exact failure this ends. There is deliberately no last_status field to reach for. `unrated` means fewer than 3 reports so no cadence can be inferred and none was declared; declare one with set_report_source to skip that state. Cadence is the median gap between a source's own report_dates unless declared; grace defaults to the cadence (floor 1 day), so a daily source goes stale on the third silent day.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    },
    "name": "list_report_sources"
  },
  {
    "description": "Declare a report source's expected cadence, its grace window, or its retirement (korg #950). Every field is an OVERRIDE -- omit one and korg keeps deriving it from the source's own report history; pass null to clear an override back to derivation. `retired:true` is how a deliberately-ended source stops nagging WITHOUT also silencing a broken one: it is an explicit declaration, so \"we turned this off\" stays distinguishable from \"this died\", which silence alone can never do. You may declare a source BEFORE its first report -- that is how a new daily source skips the `unrated` state on day one instead of spending three days in it. Returns the source's freshness row.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cadence_days": {
          "description": "Expected days between reports. `null` clears the override and returns the\nsource to inference from its own history.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "grace_days": {
          "description": "Slack past the cadence before `stale`. `null` returns it to the default,\nwhich is the cadence itself (floor 1 day).",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "note": {
          "type": [
            "string",
            "null"
          ]
        },
        "retired": {
          "description": "Mark a source deliberately ended. This is the answer to #950's open\nquestion — how a retired source stops nagging without also silencing a\nbroken one. It is an explicit declaration, so \"we turned this off\" stays\ndistinguishable from \"this died\", which silence alone can never do.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "source": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "name": "set_report_source"
  },
  {
    "description": "Create a handoff -- a durable, cross-machine context document -- and attach it to the work it describes in ONE call. `related_node_ids` are the nodes it belongs to (work items, a sprint proposal, a report, another handoff); each becomes a `has_handoff` edge. REQUIRED context, not optional related reading: the handoff surfaces automatically in those nodes' get_work_item/get_proposal `related` block. Rejects an empty `related_node_ids` unless you pass `allow_standalone` (so a forgotten link can't orphan it), and rejects the whole create if any id does not resolve (isError `not_found` -- no partial insert). Returns the handoff plus the owner ids linked.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_standalone": {
          "default": false,
          "description": "Opt in to a handoff with no owners. Off by default so a forgotten link\nstep cannot silently orphan a handoff (plan Write contract).",
          "type": "boolean"
        },
        "body": {
          "description": "The full handoff document, Markdown.",
          "type": "string"
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "related_node_ids": {
          "default": [],
          "description": "Nodes this handoff describes — its owners. Each becomes a `has_handoff`\nedge (owner -> handoff). Rejected if any id does not resolve (the whole\ncreate rolls back — a handoff must not silently lose an owner).",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        },
        "summary": {
          "minLength": 1,
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "body",
        "summary",
        "title"
      ],
      "type": "object"
    },
    "name": "create_handoff"
  },
  {
    "description": "Fetch one handoff by node_id: title, summary, full Markdown `body`, and the nodes it is attached to (`related`, both directions, capped with `related_truncated`). isError with code `not_found` if there is none. This is the authoritative read once a `has_handoff` edge points you here.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_handoff"
  },
  {
    "description": "Partially update a handoff by its node_id; returns the updated handoff (isError with code `not_found` if that node is missing or is not a handoff). Only the fields you pass change (title, summary, body, tags, archived). Relationship changes go through relate/unrelate, not here.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "body": {
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "summary": {
          "minLength": 1,
          "type": "string"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_handoff"
  },
  {
    "description": "Fetch one image attachment by `img_id` (e.g. \"img-c2a\", the display id as it appears in a markdown token) OR `node_id` -- they are the same number, hex and decimal, so pass whichever you are holding; passing both is invalid_input rather than a guess. Returns METADATA ONLY: filename, sniffed mime, byte size, pixel dimensions, content hash, the owning node ids, and a `url` per size. Bytes never travel over MCP -- to READ an image, GET the `agent` variant's url (<=1568px, Anthropic's vision ceiling; anything larger is downscaled and the extra bytes are wasted) to a temp file and read that file natively. `state` is `pending` (no owner yet -- swept 24h after upload) or `linked`, and it is DERIVED from the has_attachment edges on every read, so it cannot disagree with them. isError with code `not_found` if there is none. Every operation on the BYTES is REST, not MCP: upload with `curl -F file=@shot.png '<korg>/api/img?owner=<node_id>'`, and discard with `DELETE <korg>/api/img/<img-id>`, which is the only path that removes the record and the blobs together.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "img_id": {
          "description": "The display id, e.g. `img-c2a` — what a markdown token carries.",
          "minLength": 1,
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "description": "The attachment's node id — the same number `img_id` spells in hex.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "get_attachment"
  },
  {
    "description": "Every image attached to a node -- work item, card, proposal, anything -- oldest first, as a bare array with the same metadata get_attachment returns. get_work_item already inlines this for a work item, so reach for it when you hold some other kind of node, or after attaching. An image pasted into a COMMENT belongs to the node that comment is on: a korg comment is not a node and cannot own an edge, so the node owns the image and the comment body carries the placement token. A node with no images (or no such node) is an empty array, not an error -- this is a projection of a node's edges, and that node's own read is what says whether it exists.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "list_attachments"
  },
  {
    "description": "Answers \"which project does this work belong to?\". Returns {items, omitted:{archived}} — NOT a bare array; `omitted` is how you know rows were filtered out rather than absent. Each item is name + description (a one-line routing contract saying what belongs here and what does not) + status, the last only when it is not `active`. Defaults: active projects only, lean projection. Pass status:\"all\" to include archived (never a correct route target — their work went to a successor), detail:\"full\" for every column including machines, src_path, gh_repo, category, notes and the created/updated timestamps.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "description": "How wide. Omit for the lean routing projection (name, description, and\nstatus only when it is not `active`); `\"full\"` for every column,\nincluding `notes` and the `created`/`updated` timestamps.",
          "enum": [
            "lean",
            "full"
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Which rows. Omit for `active`; `\"all\"` to include archived.",
          "enum": [
            "active",
            "archived",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_projects"
  },
  {
    "description": "Fetch one project by name: every column — including `notes`, the long-form operational context the lean list omits, and `created`/`updated`, which say how stale this project's routing metadata is — plus the areas under it, so a focused read needs no follow-up list_areas. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "get_project"
  },
  {
    "description": "Create a project by name (idempotent — returns the existing id if it already exists). Returns its id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "create_project"
  },
  {
    "description": "Update a project's metadata by name (the name itself is immutable), returning the updated project: status (active|maintenance|inactive|archived), machines, deploy_to, category, description, notes, gh_repo, src_path. Omitted fields are unchanged. `description` is capped at 160 characters — it is a routing line, not a blurb; put anything longer in `notes`.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "enum": [
            "AI",
            "Dashboard",
            "EVAL",
            "Fun",
            "Infrastructure",
            "Ops",
            "Other",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "deploy_to": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "description": {
          "description": "The routing contract. One line, **≤160 characters** (rejected above\nthat). First clause says what work belongs here, task-shaped —\n\"harness conventions and sprint layout\", not \"a repository containing…\".\nWhere a sibling project plausibly claims the same work, add the\nboundary: \"Not X — that's `‹project›`.\" Written for an agent with zero\nprior context deciding where a work item goes. No paths, repos, hosts or\nbuild commands — those belong in the structured fields and `notes`.",
          "type": [
            "string",
            "null"
          ]
        },
        "gh_repo": {
          "type": [
            "string",
            "null"
          ]
        },
        "machines": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "notes": {
          "description": "Long-form operational context — deploy topology, build commands, house\nconventions. Unbounded, and never rendered into a routing view. Use it\nfor anything that would otherwise bloat `description` past its cap.",
          "type": [
            "string",
            "null"
          ]
        },
        "src_path": {
          "description": "Path to the working copy on the project's DEVELOPMENT machine (its\n`machines` entry), not the deploy target. Canonical form: `~/`-relative,\nno trailing slash, no whitespace, no parentheses — a path and nothing\nelse, e.g. `~/src/tools/korg`. Enforced by a CHECK constraint, so a\nvalue carrying prose or history is rejected rather than stored.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "active",
            "archived"
          ],
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "update_project"
  },
  {
    "description": "List the areas under a project (by project name), each with its `description` — what the area is for. get_project inlines the same rows, so a focused project read needs no follow-up call.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "list_areas"
  },
  {
    "description": "Create an area under a project by name (idempotent — updates the description if it already exists). Returns its id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name",
        "project"
      ],
      "type": "object"
    },
    "name": "create_area"
  },
  {
    "description": "Rename an area (`new_name`) and/or re-describe it (`description`; null clears), selected by project + current name. Returns the updated row. Work items keep pointing at the same area through a rename. isError `not_found` if no such area, `conflict` if the new name is already taken in that project.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "What the area is for; null clears it, omitted leaves it unchanged.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The area to update, by its current name.",
          "minLength": 1,
          "type": "string"
        },
        "new_name": {
          "description": "Rename to this. Omit to leave the name alone.",
          "minLength": 1,
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name",
        "project"
      ],
      "type": "object"
    },
    "name": "update_area"
  },
  {
    "description": "Delete an area by project + name. An area is a label, not a record with a history, so delete is its lifecycle end — there is no archive. REFUSES with `conflict` naming the count if any work item is still filed under it; move those off first, because deleting would silently unfile them. Returns {deleted: bool} — false means there was no such area.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name",
        "project"
      ],
      "type": "object"
    },
    "name": "delete_area"
  }
]
