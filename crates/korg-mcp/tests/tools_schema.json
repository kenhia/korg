[
  {
    "description": "Create a work item. Returns the created row (including node_id and the serial wi_number, which are the same number since the 0009 identity migration).",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "area": {
          "description": "Area name — the alternative to `area_id`, resolved within the item's project.",
          "type": [
            "string",
            "null"
          ]
        },
        "area_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "content": {
          "type": "string"
        },
        "details": {
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "sprint": {
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        },
        "wi_status": {
          "default": "open",
          "enum": [
            "open",
            "resolved",
            "done",
            "closed"
          ],
          "type": "string"
        },
        "wi_tshirt": {
          "default": "Unknown",
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "Huge",
            "Unknown"
          ],
          "type": "string"
        },
        "wi_type": {
          "default": "task",
          "enum": [
            "task",
            "bug",
            "chore",
            "feature",
            "research",
            "tweak",
            "brainstorm"
          ],
          "type": "string"
        }
      },
      "required": [
        "content",
        "title"
      ],
      "type": "object"
    },
    "name": "create_work_item"
  },
  {
    "description": "The work-item list: lean rows (wi_number, node_id, project, title, wi_type, wi_status, wi_tshirt, comment_count -- no content/details), ordered by wi_number, as {items, total, limit, offset, omitted}. `omitted` is {closed, archived} -- the rows the defaults hid, so a narrowed list can never be mistaken for the whole corpus. Defaults: everything not terminal (`open`, `resolved`, `done`) and unarchived. Pass wi_status:\"all\" (or one status, including \"closed\") for the rest, `project` (name) to scope to one project. There is no detail flag: reading one item in full is get_work_item, which also inlines its comments and edges.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "project": {
          "description": "Project name to filter by",
          "type": [
            "string",
            "null"
          ]
        },
        "wi_status": {
          "description": "Which rows. Omit for everything not terminal (`open`, `resolved`,\n`done`); `\"all\"` to include `closed` too; one status for exactly that.",
          "enum": [
            "open",
            "resolved",
            "done",
            "closed",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_work_items"
  },
  {
    "description": "Fetch a single work item by its wi_number (isError with code `not_found` if there is none), with its comments inlined (up to 10; `comments_truncated:true` + `comment_count` signal a longer thread — fetch the whole thread with list_comments, which returns every comment in one unpaginated array). Comments often hold the real payload (resolution rationale, decisions), so prefer this over list_work_items when you need the full state of one item.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "wi_number": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "wi_number"
      ],
      "type": "object"
    },
    "name": "get_work_item"
  },
  {
    "description": "Partially update a work item by its wi_number; returns the updated row (isError with code `not_found` if the wi_number does not exist). Only the fields you pass are changed. Status lifecycle: open -> resolved (implemented; may still need a user test or PR) -> done (agent satisfied; terminal but visible in default lists) -> closed (reserved for Ken; hidden from list_work_items unless you pass wi_status \"closed\" or \"all\" -- do not set unless directed). For nullable fields (project_id, details, sprint, area_id, parent, category) pass null to clear or omit to leave unchanged. Moving projects (project_id) clears an area that no longer belongs to the target project unless you pass a valid area_id in the same call.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "area": {
          "description": "Area name — the alternative to `area_id`; null clears. Resolved in the new project.",
          "type": [
            "string",
            "null"
          ]
        },
        "area_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "content": {
          "type": [
            "string",
            "null"
          ]
        },
        "details": {
          "type": [
            "string",
            "null"
          ]
        },
        "parent": {
          "description": "Parent work item's wi_number; null clears the parent.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "project": {
          "description": "Project name — the alternative to `project_id`; null unassigns. Never pass both.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "Move to this project (id); null unassigns. Get ids from list_projects.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "sprint": {
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        },
        "wi_number": {
          "format": "int64",
          "type": "integer"
        },
        "wi_status": {
          "enum": [
            "open",
            "resolved",
            "done",
            "closed"
          ],
          "type": "string"
        },
        "wi_tshirt": {
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "Huge",
            "Unknown"
          ],
          "type": "string"
        },
        "wi_type": {
          "enum": [
            "task",
            "bug",
            "chore",
            "feature",
            "research",
            "tweak",
            "brainstorm"
          ],
          "type": "string"
        }
      },
      "required": [
        "wi_number"
      ],
      "type": "object"
    },
    "name": "update_work_item"
  },
  {
    "description": "Create a kanban card. Returns the created card row.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "default": "",
          "type": "string"
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "default": 0,
          "type": "number"
        },
        "status": {
          "default": "Backlog",
          "enum": [
            "Backlog",
            "Research",
            "OnDeck",
            "Active",
            "Done",
            "Cut"
          ],
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "type": "object"
    },
    "name": "create_card"
  },
  {
    "description": "Partially update a kanban card by its node_id; returns the updated card (isError with code `not_found` if that node is missing or is not a card). Projects are addressed by `project_id` here and over REST alike (get ids from list_projects) -- REST used to take a project *name* and silently create it. Only the fields you pass are changed (move status/rank, edit title/description, archive, reassign project). For nullable fields (project_id, category) pass null to clear or omit to leave unchanged.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "project": {
          "description": "Project name — the alternative to `project_id`; null unassigns. Never pass both.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "type": "number"
        },
        "status": {
          "enum": [
            "Backlog",
            "Research",
            "OnDeck",
            "Active",
            "Done",
            "Cut"
          ],
          "type": "string"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_card"
  },
  {
    "description": "List cards as {items, total, limit, offset}, ordered by status, then rank, then node_id. Each row includes `comment_count`. Archived cards are EXCLUDED by default.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "project": {
          "description": "Project name to filter by",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "Backlog",
            "Research",
            "OnDeck",
            "Active",
            "Done",
            "Cut",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_cards"
  },
  {
    "description": "List the comments on a node (work item or card), oldest first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "list_comments"
  },
  {
    "description": "Add a comment to a node of any kind. Returns the created comment; isError with code `not_found` if the node does not exist.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "minLength": 1,
          "type": "string"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "body",
        "node_id"
      ],
      "type": "object"
    },
    "name": "add_comment"
  },
  {
    "description": "Delete a comment by its id. Returns {deleted: bool} — false means there was no such comment.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "name": "delete_comment"
  },
  {
    "description": "Edit a comment's body by its id (from list_comments). `created` is preserved; `updated` advances.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "minLength": 1,
          "type": "string"
        },
        "id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "body",
        "id"
      ],
      "type": "object"
    },
    "name": "update_comment"
  },
  {
    "description": "Capture a reading-list URL. Returns the created link row.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "name": "create_link"
  },
  {
    "description": "List reading-list links as {items, total, limit, offset}, ordered by node_id. Archived links are EXCLUDED by default. Each row carries `created` and `updated`, so capture recency is readable without a second call.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "disposition": {
          "enum": [
            "Unread",
            "Done",
            "Revisit",
            "Summarized",
            "VaultSaved",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "read": {
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_links"
  },
  {
    "description": "Update a reading-list link in ONE transaction: disposition, read flag, tags, archived -- any combination. This is how an agent records what it decided about a captured URL (migration 0004's intended workflow), and `archived` is its lifecycle end: the disposal list_links has always excluded by default. Returns the updated link; isError `not_found` if the node is missing or is not a link; an invalid disposition changes nothing.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "description": "The lifecycle end for a link that was real (WI #888). `list_links` has\ndocumented an archived-excluded default since #534 with no write able\nto reach it; this is that write. For a capture that was never real —\na probe, a mistyped URL — `delete_link` is the honest disposal.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "disposition": {
          "enum": [
            "Unread",
            "Done",
            "Revisit",
            "Summarized",
            "VaultSaved"
          ],
          "type": "string"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "read": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_link"
  },
  {
    "description": "Hard-delete a captured URL -- the disposal for a link that was never real (a probe, a mistyped capture). For a link that WAS real, archive it with update_link instead; delete is irreversible. REFUSES with `conflict` if the link has any relationship edge, comment, or daily-plan reference, naming what points at it -- anything referenced has a history, and resolving that reference is your decision, not the database's. Returns {deleted: bool} -- false means there was no such link.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "delete_link"
  },
  {
    "description": "DEPRECATED -- use update_link, which does this plus disposition and tags in one transaction. Marks a reading-list link read or unread; returns the updated link.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "read": {
          "type": "boolean"
        }
      },
      "required": [
        "node_id",
        "read"
      ],
      "type": "object"
    },
    "name": "mark_link_read"
  },
  {
    "description": "Create a relationship edge between any two nodes. The label reads left-to-right. The vocabulary is CLOSED -- these five labels and no others: `covers` (proposal -> work item), `finding` (report -> work item), `depends_on` (dependent -> dependency), and `has_handoff` (node -> handoff; normally written by create_handoff, but relate attaches an existing handoff to another node) are DIRECTED -- orientation is meaningful, and the reverse is a distinct edge (A depends_on B plus B depends_on A is a cycle, not a duplicate). `related-to` is UNDIRECTED -- orientation is stored but meaningless, so read it symmetrically. An unregistered label is invalid_input naming the registry and the near-miss; `covers`, `finding`, and `has_handoff` also validate endpoint kinds (`has_handoff`'s right end must be a handoff), and `covers` additionally requires BOTH ENDS IN THE SAME PROJECT -- a proposal bundles work from one project, and cross-project work belongs to a program, the layer above proposals (korg #968). `depends_on` across projects stays legal and is the normal case. Exact duplicates dedup, and relating the reverse of an undirected edge returns the existing one. Optionally pass `origin` -- self-reported provenance (e.g. your skill name); it is recorded, not verified. Both endpoints must exist (isError `not_found`) and must differ (isError `invalid_input` -- self-edges are rejected).",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "minLength": 1,
          "type": "string"
        },
        "left": {
          "format": "int64",
          "type": "integer"
        },
        "origin": {
          "description": "Self-reported provenance (D-17): who is writing this edge — the web\nclient sends `\"web\"`, a skill sends its name. Optional and unverified\n(korg is no-auth HTTP); absent means unknown, stored NULL.",
          "type": [
            "string",
            "null"
          ]
        },
        "right": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "label",
        "left",
        "right"
      ],
      "type": "object"
    },
    "name": "relate"
  },
  {
    "description": "List the nodes linked to a node (any kind), with labels. Returns {items, total, limit, truncated}. Each item has `rel_id` (pass to `unrelate`), `direction` (\"out\" = the queried node is the edge's left, so the label reads queried->neighbor; \"in\" = the reverse) and `directed` -- when `directed` is false the label is registry-undirected (e.g. related-to) and you MUST treat the edge as symmetric, ignoring `direction`. Filter server-side with `label` and/or `kind` instead of pulling every edge: e.g. label=\"covers\", kind=\"workitem\" for a proposal's work items. Ordering is neighbor node_id then rel_id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "description": "Only neighbors of this node kind, e.g. \"workitem\".",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "Only edges with this relationship label.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "default": 100,
          "description": "Cap on returned edges; `truncated` says whether more matched.",
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "neighbors"
  },
  {
    "description": "Remove a relationship edge by its id (the `rel_id` from `neighbors`, or the id returned by `relate`). Returns {deleted: bool} — false means there was no such edge.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "name": "unrelate"
  },
  {
    "description": "Create a reusable planning topic. Returns the created topic.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "create_topic"
  },
  {
    "description": "Fetch a topic by node_id, including archived topics. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_topic"
  },
  {
    "description": "List topics as {items, total, limit, offset}, ordered by name. Pass `q` to match name/description. Each row includes `comment_count`. Archived topics are EXCLUDED by default.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "q": {
          "description": "Match against name and description",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_topics"
  },
  {
    "description": "Alias for list_topics with a `q` filter; same {items, total, limit, offset} shape. Prefer list_topics.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "default": 200,
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "q": {
          "description": "Match against name and description",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "search_topics"
  },
  {
    "description": "Partially update a topic; returns the updated topic.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_topic"
  },
  {
    "description": "Archive or restore a topic; returns the updated topic.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": true,
          "type": "boolean"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "archive_topic"
  },
  {
    "description": "List daily plan items in an inclusive date range with snapshots and current source titles.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "from": {
          "description": "YYYY-MM-DD",
          "type": "string"
        },
        "to": {
          "description": "YYYY-MM-DD",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "name": "list_daily_plan"
  },
  {
    "description": "Plan a work item, card, topic, or sprint proposal. Display is resolved and snapshotted server-side. Returns the created item. `plan_date` must be the server's local today or later -- that date is derived from the server's configured timezone, NOT from your clock, and a refusal names it.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "plan_date": {
          "description": "YYYY-MM-DD",
          "type": "string"
        },
        "source_node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "plan_date",
        "source_node_id"
      ],
      "type": "object"
    },
    "name": "create_daily_plan_item"
  },
  {
    "description": "Complete or uncomplete any daily plan item; timestamp is server-authoritative. Returns the updated item.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "completed": {
          "type": "boolean"
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "completed",
        "node_id"
      ],
      "type": "object"
    },
    "name": "set_daily_plan_completion"
  },
  {
    "description": "Delete an item from an open day; past structure is frozen -- a day before the server's local today is a sealed record and is refused with conflict, naming that date. Returns {deleted: true}.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "delete_daily_plan_item"
  },
  {
    "description": "Replace the complete order for an open day -- the server's local today or later; a past day is frozen (conflict, naming the date). Returns the day in its new order.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_ids": {
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        },
        "plan_date": {
          "description": "YYYY-MM-DD",
          "type": "string"
        }
      },
      "required": [
        "node_ids",
        "plan_date"
      ],
      "type": "object"
    },
    "name": "reorder_daily_plan"
  },
  {
    "description": "Move an item to the server's local today or a future day (a past target is refused, naming that date). Open sources transfer; past sources copy and remain unchanged.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "target_date": {
          "description": "YYYY-MM-DD",
          "type": "string"
        },
        "target_position": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "node_id",
        "target_date"
      ],
      "type": "object"
    },
    "name": "move_daily_plan_item"
  },
  {
    "description": "Return all complete and incomplete historical items plus completion totals/rate. End must be before the server's local today, which is derived from its configured timezone rather than your clock; a refusal names that date.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "from": {
          "description": "YYYY-MM-DD",
          "type": "string"
        },
        "source_node_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "to": {
          "description": "YYYY-MM-DD",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "name": "daily_plan_history"
  },
  {
    "description": "Propose a sprint: bundle a title + summary with the work items it covers, in one call. `project` (or `project_id`) is REQUIRED, and every covered work item must be in it -- a proposal is a single-project bundle, so one from another project is invalid_input naming both projects, not a silent drop. Cross-project work is bundled by a program, the layer above proposals (korg #968). `summary` is a routing contract capped at 500 characters -- what the bundle is, why now, roughly how big -- and the analysis goes in `notes`, which is unbounded; an over-long summary is invalid_input, not a truncation. Returns the created proposal plus `covered` -- which of the given wi_numbers actually resolved. Numbers that do not resolve are dropped, so compare `covered` against your request.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "notes": {
          "description": "The analysis — measurements, dependencies, sequencing, alternatives\nconsidered. Unbounded, and the home for everything that will not fit\n`summary`. Returned by `get_proposal` and `list_proposals detail:\"full\"`,\nnever by the lean queue row.",
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "default": false,
          "type": "boolean"
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth, and **one of them is required**. Resolved by exact name, and an\nunknown name returns `not_found` rather than mis-filing, so pass a name\nyou are confident in directly. Call `list_projects` only when the name is\ngenuinely unknown or ambiguous — the roster in this server's\ninstructions already names every active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "description": "**Required** — as either this or `project`, never both. A proposal is a\nsingle-project bundle (#967): it is what tells a session which repo to\nbranch in, and what `covers` is validated against.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "rank": {
          "default": 0,
          "description": "Drag-order position; lower sorts first among unpinned proposals.",
          "type": "number"
        },
        "summary": {
          "description": "The routing contract. **≤500 characters** (rejected above that): what\nthis bundle is, why now, roughly how big. Written for a session choosing\na sprint from a queue, not for one that has already chosen it.",
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        },
        "work_item_numbers": {
          "default": [],
          "description": "wi_numbers this proposal covers; numbers that don't resolve are dropped.\nA number that resolves to a work item in a **different project** is not\ndropped — it is `invalid_input` naming both projects (#967), because\nsilently omitting the item you meant to include is the failure this rule\nexists to stop.",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        }
      },
      "required": [
        "summary",
        "title"
      ],
      "type": "object"
    },
    "name": "propose_sprint"
  },
  {
    "description": "Create or replace the daily report for (source, report_date). Same-day re-runs REPLACE both the content and the finding set -- findings you omit are unlinked -- but keep the node_id (links/comments survive). `findings_linked` echoes the wi_numbers that resolved; numbers that do not resolve are dropped, so compare it against your request.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "full markdown report",
          "type": "string"
        },
        "escalated": {
          "default": false,
          "type": "boolean"
        },
        "finding_work_items": {
          "default": [],
          "description": "wi_numbers of finding work items; numbers that don't resolve are dropped.",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "report_date": {
          "description": "YYYY-MM-DD",
          "format": "date",
          "type": "string"
        },
        "source": {
          "description": "reporter id, e.g. 'kmon'",
          "minLength": 1,
          "type": "string"
        },
        "status": {
          "enum": [
            "ok",
            "attention",
            "problem"
          ],
          "type": "string"
        },
        "summary": {
          "description": "one-liner for the list view",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "body",
        "report_date",
        "source",
        "status",
        "summary"
      ],
      "type": "object"
    },
    "name": "create_report"
  },
  {
    "description": "List daily reports, newest first (summary fields only). Pass `source` to filter.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "limit": {
          "default": 30,
          "type": "integer"
        },
        "source": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_reports"
  },
  {
    "description": "Fetch one report by node_id: full body plus linked finding work items. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_report"
  },
  {
    "description": "The sprint queue: pinned first, then rank, then node_id (a stable order -- equal ranks no longer shuffle between calls). Returns {items, omitted} -- NOT a bare array; `omitted` counts the rows the defaults hid ({done, declined, archived}), so a narrowed queue can never be mistaken for the whole corpus. Defaults: live proposals only (`proposed` + `active`), unarchived, and the lean projection -- node_id, title, status, project, rank, pinned, covered_count, comment_count, no `summary`. Pass status:\"all\" (or one status) for the rest, detail:\"full\" for every column including `summary` and `notes`. Reading one proposal is get_proposal, which also gives you the work items it covers.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "default": false,
          "description": "Omit for unarchived only (the default); true for archived only; null for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detail": {
          "enum": [
            "lean",
            "full"
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name to filter by",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "proposed",
            "active",
            "done",
            "declined",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_proposals"
  },
  {
    "description": "Fetch one sprint proposal by node_id with everything needed to start it: the proposal fields (including `notes`, the full analysis the 500-character `summary` cannot hold), `covered` (the work items it covers -- wi_number, node_id, title, wi_status, wi_tshirt, project, comment_count -- ordered by wi_number), and inlined comments (up to 10, with `comments_truncated`). This replaces the old list_proposals + neighbors + list_work_items dance. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_proposal"
  },
  {
    "description": "Partially update a sprint proposal by its node_id; returns the updated proposal (isError with code `not_found` if that node is missing or is not a proposal). Only the fields you pass are changed. Use this for status transitions (proposed -> active -> done/declined), reordering (rank), pinning, or archiving. `summary` is capped at 500 characters (invalid_input above that) -- long-form analysis belongs in `notes`, which is unbounded; pass null to clear it.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "notes": {
          "description": "The analysis: measurements, dependencies, sequencing, what was\nconsidered and rejected. Unbounded; pass `null` to clear it.",
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "rank": {
          "type": "number"
        },
        "status": {
          "enum": [
            "proposed",
            "active",
            "done",
            "declined"
          ],
          "type": "string"
        },
        "summary": {
          "description": "The routing contract, **≤500 characters** (rejected above that). Put the\nanalysis in `notes` — it is unbounded, and that is what it is for.",
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_proposal"
  },
  {
    "description": "Create a handoff -- a durable, cross-machine context document -- and attach it to the work it describes in ONE call. `related_node_ids` are the nodes it belongs to (work items, a sprint proposal, a report, another handoff); each becomes a `has_handoff` edge. REQUIRED context, not optional related reading: the handoff surfaces automatically in those nodes' get_work_item/get_proposal `related` block. Rejects an empty `related_node_ids` unless you pass `allow_standalone` (so a forgotten link can't orphan it), and rejects the whole create if any id does not resolve (isError `not_found` -- no partial insert). Returns the handoff plus the owner ids linked.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_standalone": {
          "default": false,
          "description": "Opt in to a handoff with no owners. Off by default so a forgotten link\nstep cannot silently orphan a handoff (plan Write contract).",
          "type": "boolean"
        },
        "body": {
          "description": "The full handoff document, Markdown.",
          "type": "string"
        },
        "category": {
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project name, e.g. `klams` — the alternative to `project_id`; never pass\nboth. Resolved by exact name, and an unknown name returns `not_found`\nrather than mis-filing, so pass a name you are confident in directly.\nCall `list_projects` only when the name is genuinely unknown or\nambiguous — the roster in this server's instructions already names\nevery active project.",
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "related_node_ids": {
          "default": [],
          "description": "Nodes this handoff describes — its owners. Each becomes a `has_handoff`\nedge (owner -> handoff). Rejected if any id does not resolve (the whole\ncreate rolls back — a handoff must not silently lose an owner).",
          "items": {
            "format": "int64",
            "type": "integer"
          },
          "type": "array"
        },
        "summary": {
          "minLength": 1,
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "body",
        "summary",
        "title"
      ],
      "type": "object"
    },
    "name": "create_handoff"
  },
  {
    "description": "Fetch one handoff by node_id: title, summary, full Markdown `body`, and the nodes it is attached to (`related`, both directions, capped with `related_truncated`). isError with code `not_found` if there is none. This is the authoritative read once a `has_handoff` edge points you here.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "node_id": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_handoff"
  },
  {
    "description": "Partially update a handoff by its node_id; returns the updated handoff (isError with code `not_found` if that node is missing or is not a handoff). Only the fields you pass change (title, summary, body, tags, archived). Relationship changes go through relate/unrelate, not here.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "body": {
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "format": "int64",
          "type": "integer"
        },
        "summary": {
          "minLength": 1,
          "type": "string"
        },
        "tags": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "node_id"
      ],
      "type": "object"
    },
    "name": "update_handoff"
  },
  {
    "description": "Answers \"which project does this work belong to?\". Returns {items, omitted:{archived}} — NOT a bare array; `omitted` is how you know rows were filtered out rather than absent. Each item is name + description (a one-line routing contract saying what belongs here and what does not) + status, the last only when it is not `active`. Defaults: active projects only, lean projection. Pass status:\"all\" to include archived (never a correct route target — their work went to a successor), detail:\"full\" for every column including machines, src_path, gh_repo, category, notes and the created/updated timestamps.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "description": "How wide. Omit for the lean routing projection (name, description, and\nstatus only when it is not `active`); `\"full\"` for every column,\nincluding `notes` and the `created`/`updated` timestamps.",
          "enum": [
            "lean",
            "full"
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Which rows. Omit for `active`; `\"all\"` to include archived.",
          "enum": [
            "active",
            "archived",
            "all"
          ],
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "name": "list_projects"
  },
  {
    "description": "Fetch one project by name: every column — including `notes`, the long-form operational context the lean list omits, and `created`/`updated`, which say how stale this project's routing metadata is — plus the areas under it, so a focused read needs no follow-up list_areas. isError with code `not_found` if there is none.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "get_project"
  },
  {
    "description": "Create a project by name (idempotent — returns the existing id if it already exists). Returns its id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "create_project"
  },
  {
    "description": "Update a project's metadata by name (the name itself is immutable), returning the updated project: status (active|maintenance|inactive|archived), machines, deploy_to, category, description, notes, gh_repo, src_path. Omitted fields are unchanged. `description` is capped at 160 characters — it is a routing line, not a blurb; put anything longer in `notes`.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "enum": [
            "AI",
            "Dashboard",
            "EVAL",
            "Fun",
            "Infrastructure",
            "Ops",
            "Other",
            null
          ],
          "type": [
            "string",
            "null"
          ]
        },
        "deploy_to": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "description": {
          "description": "The routing contract. One line, **≤160 characters** (rejected above\nthat). First clause says what work belongs here, task-shaped —\n\"harness conventions and sprint layout\", not \"a repository containing…\".\nWhere a sibling project plausibly claims the same work, add the\nboundary: \"Not X — that's `‹project›`.\" Written for an agent with zero\nprior context deciding where a work item goes. No paths, repos, hosts or\nbuild commands — those belong in the structured fields and `notes`.",
          "type": [
            "string",
            "null"
          ]
        },
        "gh_repo": {
          "type": [
            "string",
            "null"
          ]
        },
        "machines": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "notes": {
          "description": "Long-form operational context — deploy topology, build commands, house\nconventions. Unbounded, and never rendered into a routing view. Use it\nfor anything that would otherwise bloat `description` past its cap.",
          "type": [
            "string",
            "null"
          ]
        },
        "src_path": {
          "description": "Path to the working copy on the project's DEVELOPMENT machine (its\n`machines` entry), not the deploy target. Canonical form: `~/`-relative,\nno trailing slash, no whitespace, no parentheses — a path and nothing\nelse, e.g. `~/src/tools/korg`. Enforced by a CHECK constraint, so a\nvalue carrying prose or history is rejected rather than stored.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "enum": [
            "active",
            "archived"
          ],
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "update_project"
  },
  {
    "description": "List the areas under a project (by project name), each with its `description` — what the area is for. get_project inlines the same rows, so a focused project read needs no follow-up call.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "list_areas"
  },
  {
    "description": "Create an area under a project by name (idempotent — updates the description if it already exists). Returns its id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name",
        "project"
      ],
      "type": "object"
    },
    "name": "create_area"
  },
  {
    "description": "Rename an area (`new_name`) and/or re-describe it (`description`; null clears), selected by project + current name. Returns the updated row. Work items keep pointing at the same area through a rename. isError `not_found` if no such area, `conflict` if the new name is already taken in that project.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "What the area is for; null clears it, omitted leaves it unchanged.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The area to update, by its current name.",
          "minLength": 1,
          "type": "string"
        },
        "new_name": {
          "description": "Rename to this. Omit to leave the name alone.",
          "minLength": 1,
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name",
        "project"
      ],
      "type": "object"
    },
    "name": "update_area"
  },
  {
    "description": "Delete an area by project + name. An area is a label, not a record with a history, so delete is its lifecycle end — there is no archive. REFUSES with `conflict` naming the count if any work item is still filed under it; move those off first, because deleting would silently unfile them. Returns {deleted: bool} — false means there was no such area.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "type": "string"
        },
        "project": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "name",
        "project"
      ],
      "type": "object"
    },
    "name": "delete_area"
  }
]
